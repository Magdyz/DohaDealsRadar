# Edge Function settings
# All functions verify a JWT at the gateway (anon key or user session).
# Identity and roles are then checked inside each function from the user's JWT.

[functions.maintenance]
# Called by pg_cron via pg_net; authenticated with the CRON_SECRET header instead.
verify_jwt = false
