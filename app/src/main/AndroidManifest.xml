<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internet for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera is needed only for TakePicture; gallery picker needs no storage permission on API 26+ -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ✅ NEW: Push notifications (Android 13+) (2025-11-25) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Photos come from the system photo picker: no storage/media permission needed -->

    <application
        android:enableOnBackInvokedCallback="true"
        android:name=".EgyptDealsApp"
        android:resizeableActivity="true"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EgyptDealRadar"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- FileProvider for camera URI -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ✅ NEW: Firebase Cloud Messaging Service (2025-11-25) -->
        <service
            android:name=".fcm.EgyptDealsFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- ✅ NEW: Default notification channel (for FCM) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="deals_channel" />

    </application>

</manifest>
